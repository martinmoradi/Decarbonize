import React from 'react';
import { View, Text } from 'react-native';
import HistoryNavigator from '../../routers/HistoryNavigator';

const HistoryContainer = () => {
  return <HistoryNavigator />;
};

export default HistoryContainer;
